<Window x:Class="Sound2Light.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sound2Light"
        xmlns:units="clr-namespace:Sound2Light.Views.Controls.Units"
        mc:Ignorable="d"
        Title="Sound2Light" Height="600" Width="1000">

    <!-- 🎨 Hintergrundbild -->
    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/Assets/Images/AppBackground.png"
                    Stretch="UniformToFill" />
    </Window.Background>

    <!-- 🧱 UI-Layout -->
    <Grid Margin="0">
        <!-- 4x4 Gitterstruktur -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 🔧 Zeile 0 -->
        <units:UnitSetup         Grid.Row="0" Grid.Column="0" Margin="0"/>

    </Grid>
</Window>
