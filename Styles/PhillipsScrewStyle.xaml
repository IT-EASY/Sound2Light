<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:Sound2Light.Services.UI">

    <Style x:Key="PhillipsScrewStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="20" />
        <Setter Property="Height" Value="20" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Fill">
            <Setter.Value>
                <RadialGradientBrush GradientOrigin="0.3,0.3" Center="0.5,0.5" RadiusX="0.5" RadiusY="0.5">
                    <GradientStop Color="#999" Offset="0.0"/>
                    <GradientStop Color="#444" Offset="1.0"/>
                </RadialGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Stroke" Value="#222" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Opacity="0.5" />
            </Setter.Value>
        </Setter>

        <!-- Korrekte Notation für Attached Property -->
        <Setter Property="ui:RotationScrewService.InitializeRandomRotation"
                Value="True" />
    </Style>

</ResourceDictionary>
